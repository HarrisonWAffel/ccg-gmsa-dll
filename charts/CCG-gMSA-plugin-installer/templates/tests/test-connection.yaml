apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CCG-gMSA-plugin-installer.fullname" . }}-test-connection"
  labels:
    {{- include "CCG-gMSA-plugin-installer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "CCG-gMSA-plugin-installer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
